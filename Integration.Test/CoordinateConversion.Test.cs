using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Integration.Test
{
    [TestClass]
    public class CoordinateConversion
    {
        [TestMethod]
        public void cartesiantoCylindrical_coords0()
        {
            //var viewModel = new CoordinateViewModel();

            //double[] res_coords0 = CoordinateConverter.cartesianToCylindrical(viewModel.coords0);
            //double[] answer_coords0 = new double[] { 0, 0, 0 };
            //for(int i = 0; i < 3; i++)
            //{
            //    Assert.AreEqual(res_coords0[i], answer_coords0[i]);
            //}
        }
    }
}
